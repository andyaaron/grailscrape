import * as d3 from 'd3';
const calculateTrendLine = (data) => {
//     const xValues = data.map((d) => d.sold_at);
//     const yValues = data.map((d) => d.sold_price);
//     const regression = d3.regressionLinear()(xValues.map((d, i) => [d, yValues[i]]));
//     return trendLineData;
};

export default calculateTrendLine;
